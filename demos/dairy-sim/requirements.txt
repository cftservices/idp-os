# The dashboard runs with ZERO dependencies (stdlib http.server) -> `python sim.py`.
# These are only needed for the optional Connect-layer paths:
paho-mqtt>=2.0      # optional: publish telemetry to MonsterMQ (set MQTT_HOST)
asyncua>=1.0        # optional: OPC-UA serve/read mirroring the dairy contract
